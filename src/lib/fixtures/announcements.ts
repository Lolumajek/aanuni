// DEMO DATA — placeholder announcement content for local development.
import type { Announcement } from "@/lib/content/schemas/announcement";

export const announcementsFixture: Announcement[] = [
  {
    id: "demo-free-shipping",
    message:
      "Free standard shipping on orders over $75 — demo content, not a live offer.",
    enabled: true,
  },
];
